export const ANIMATION_DURATION = {
  fast: 150,
  normal: 200,
  slow: 300,
  scroll: 600,
  countUp: 1500,
} as const;

export const INTERSECTION_THRESHOLD = {
  card: 0.1,
  counter: 0.5,
} as const;

export const MARQUEE_SPEED = 30; // seconds per loop
export const STAGGER_DELAY = 80; // ms per card in staggered grid
